import React from 'react';
import GoBack from '../../components/GoBack/GoBack';

const Help = ({ history }) => {
	return (
		<div>
			Q&amp;A
			<GoBack history={history} />
		</div>);
};

export default Help;
